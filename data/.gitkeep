# D&D 5e API Data Cache

This directory holds cached JSON files from the D&D 5e API.

The files are generated at deploy time by .github/workflows/static.yml
(via scripts/fetch-dnd-data.sh) and are gitignored — they are not committed.

For local development, run scripts/fetch-dnd-data.sh once to populate this
directory before serving the site.
